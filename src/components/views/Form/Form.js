import React from 'react';
import styles from './Form.module.scss';

const Form = () => {
  return (
    <div className={styles.component}>
      I am the form;
    </div>
  );
};

export default Form;